let library = [];

function Book(title, author, series, pages, isRead){
    this.title = title;
    this.author = author;
    this.series = series;
    this.pages = pages;
    this.isRead = isRead;
}

